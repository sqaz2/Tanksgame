# StarMuff Skirmish

Space-themed artillery duel inspired by Scorched Earth/Pocket Tanks for starmuff.com.

## Run

Open `index.html` in a browser, or run a simple server:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000`.
